streamlit==1.41.0
opencv-python-headless==4.9.0.80  # No libGL dependency
torch==2.2.1+cpu  # CPU-only version (no CUDA needed)
torchvision==0.17.1+cpu
numpy==1.26.0  # Stable version (not numpy 2.0 beta)
pillow==10.2.0  # For image processing
tqdm==4.66.2  # Progress bars
